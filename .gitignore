
# pixi environments
.pixi
*.egg-info
# magic environments
.magic

# wasm build outputs (regenerated by scripts/build-all.sh)
/build/

# golden LLVM-IR baseline is toolchain-specific: bootstrapped per environment
# by scripts/ir-snapshot.sh, not committed
/toolchain/ir/golden/

# node
node_modules/

